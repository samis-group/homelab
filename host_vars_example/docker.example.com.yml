---
vm:
  name: docker   # Required - This must match the subdomain name exactly as DNS records get created from this (i.e. <name>.domain.com)
  ip_address: "{{ vm_defaults.network }}20"   # Required
  id: 101   # Required
  hdd_size: "100G"
  boot_disk: sata0
