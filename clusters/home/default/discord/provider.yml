---
apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Provider
metadata:
  name: discord
  namespace: default
spec:
  channel: homelab
  secretRef:
    name: discord-webhook
  type: discord
  username: FluxBot
