---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dev-all
  namespace: kube-system
spec:
  secretStoreRef:
    kind: SecretStore
    name: doppler-auth-api

  target:
    name: dev-all

  dataFrom:
    - find:
        name:
          regexp: .*
