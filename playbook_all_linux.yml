---
####################################
### Run tasks on all linux hosts ###
####################################

- name: Run tasks on all linux hosts
  hosts: docker wsl proxmox
  become: true

  tasks:

    - name: Set timezone
      timezone:
        name: "{{ tz }}"
