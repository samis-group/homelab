---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: default
spec:
  # ipFamilyPolicy: PreferDualStack
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
